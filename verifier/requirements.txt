fastapi==0.115.5
uvicorn[standard]==0.32.1
httpx==0.27.2
cryptography==43.0.3
pydantic==2.9.2
pydantic-settings==2.6.1
structlog==24.4.0
sentry-sdk[fastapi]==2.18.0
jinja2==3.1.4
python-multipart==0.0.17
slowapi==0.1.9
# RFC 8785 JCS — MUST be the same exact pin as backend/requirements.txt
# (signature bytes are produced by this library on the backend side).
rfc8785==0.1.4

# --- dev / test (installed locally; production image ignores via constraints if needed) ---
pytest==8.3.3
pytest-asyncio==0.24.0
respx==0.21.1
ruff==0.7.4
