// One-line shim — `@/components/EmptyState` is the legacy path.
// New code SHOULD import `EmptyState` from `@/components/ui`.
export { default, EmptyState } from '@/components/ui/EmptyState';
export type {
  EmptyStateProps,
  EmptyStateAction,
} from '@/components/ui/EmptyState';
