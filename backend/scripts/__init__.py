"""Realistic seed scripts (L3.2).

This package is **importable** from inside the backend test suite — e.g.::

    from scripts.seed_realistic import seed
    from scripts.seed_realistic_reset import reset

The wider repo also ships a top-level ``scripts/`` directory (one level up)
for dev-environment tooling like ``dev_setup.py``; that directory is
independent and not affected by anything here.
"""

from __future__ import annotations
